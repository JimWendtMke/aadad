export interface GuxTabPanelContentType {
  /**
  * When present and true, tab content will be HTML
  */
  isHTML?: boolean;
  /**
  * Defines the content to display in the tab
  */
  content: any;
}
